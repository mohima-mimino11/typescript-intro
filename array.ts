let skills : string[] = [];
skills[0] = 'Programming';
skills[1] = 'Problem Solving';
skills.push('system design');
console.log(skills);
let scores : (string | number)[] = ['Programming', 5, 'system design', 2];
console.log(scores);
// tuples
let skill: [string, number];
skill = ['Programming', 5];
console.log(skill);

let color: [number, number, number] = [255, 0, 0];
console.log(color);
